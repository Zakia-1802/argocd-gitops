apiVersion: v1
kind: Service
metadata:
  name: {{ include "argoapps.fullname" . }}
  labels:
    {{- include "argoapps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argoapps.selectorLabels" . | nindent 4 }}
